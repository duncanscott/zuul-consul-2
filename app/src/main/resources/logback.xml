<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <variable name="ROOT_LOG_LEVEL" value="${ROOT_LOG_LEVEL:-INFO}"/>
    <property name="applicationName" value="zuul-consul"/>
    <property name="DefaultPattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${DefaultPattern}</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender -->
    <property name="LOG_FILE" value="${user.home}/logs/${applicationName}"/>
    <appender name="ROLLER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>21</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>${DefaultPattern}</pattern>
        </encoder>
    </appender>

    <!-- JSON appender for log aggregation (ELK/Logstash/Splunk) -->
    <property name="LOG_FILE_JSON" value="${user.home}/json-logs/${applicationName}-json"/>
    <appender name="JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_JSON}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${LOG_FILE_JSON}.log.%i</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>21</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>10MB</MaxFileSize>
        </triggeringPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timeZone>PST</timeZone>
            <customFields>{"application-name":"${applicationName}"}</customFields>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="${ROOT_LOG_LEVEL}">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ROLLER"/>
        <appender-ref ref="JSON"/>
    </root>

    <!-- Application logging -->
    <logger name="org.jadetipi.zuulconsul" level="DEBUG"/>

    <!-- Filter logging -->
    <logger name="inbound" level="DEBUG"/>
    <logger name="outbound" level="DEBUG"/>

    <!-- Reduce noisy loggers -->
    <logger name="com.netflix.config" level="WARN"/>
    <logger name="com.netflix.discovery" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="io.vertx" level="INFO"/>
</configuration>
